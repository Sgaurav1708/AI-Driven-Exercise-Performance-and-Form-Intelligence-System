from pathlib import Path
p=Path('README.md');s=p.read_text(encoding='utf-8');s=s.replace('- Six bundled, original animated movement-diagram videos with playback controls.','- Six bundled animated guides with numbered steps, tracking-joint highlights and angle annotations. These are diagrams, not filmed trainer demonstrations.\n- A 1.5-second camera readiness check before each camera set and after resuming; counting waits for visible joints and a stable start position.\n- Adjustable 0–300 second rest countdown between sets, optional spoken completion cue and manual start/skip.\n- Seven-day live workout totals and a daily activity chart, excluding simulated sessions.')
s=s.replace('Wait for **Offline pack ready** before disconnecting.', 'Watch the file-by-file download progress and wait for **Offline pack ready** before disconnecting. The pack is stored inside the browser, not in the Downloads folder.')
s=s.replace('Male/female voice availability depends', 'An offline-only filter and empty-list guidance help select a usable voice. Male/female voice availability depends')
s=s.replace('- `dist/engine.js`: measurement and exercise tracking rules.', '- `dist/engine.js`: measurement and exercise tracking rules.\n- `dist/training.js`: readiness gating, rest timing and weekly progress aggregation.')
p.write_text(s,encoding='utf-8')
p=Path('DEMO-GUIDE.md');s=p.read_text(encoding='utf-8').replace('Follow the exercise-specific camera setup.','Follow the exercise-specific camera setup and hold the starting position until the readiness check completes.').replace('After each set, rest and choose Start next set.', 'Choose a rest duration before starting. After each set, the rest timer counts down; choose Start next set when ready, or skip the remaining rest.').replace('Workout history includes CSV export.', 'Workout history includes a seven-day progress summary and CSV export.');p.write_text(s,encoding='utf-8')
