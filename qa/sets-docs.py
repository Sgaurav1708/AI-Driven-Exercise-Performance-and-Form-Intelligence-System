from pathlib import Path
p=Path('README.md');s=p.read_text(encoding='utf-8').replace('- Camera overlay, angle chart, targets, pause/resume, local workout history and CSV export.', '- Set targets (1–20) with reps or hold seconds per set. Counting pauses at each target; Start next set resumes after your rest. History and CSV include completed sets and total results.\n- Camera overlay, angle chart, targets, pause/resume, local workout history and CSV export.');p.write_text(s,encoding='utf-8')
p=Path('DEMO-GUIDE.md');s=p.read_text(encoding='utf-8').replace('Set a rep target, or a seconds target for plank.','Set the number of sets and a rep target per set, or a seconds target per set for plank.').replace('7. Finish the session to save it.', '7. After each set, rest and choose Start next set. Finish the session after your final set, or finish early to save partial progress.');p.write_text(s,encoding='utf-8')
p=Path('VERIFICATION.md');s=p.read_text(encoding='utf-8');s+='\nSet tracking update: browser checks passed for two full squat sets, two timed plank sets, paused counting during rest, counter reset between sets, early finish without a completed set, persisted set totals and mobile layout. All 16 tracking tests still pass.\n';p.write_text(s,encoding='utf-8')
